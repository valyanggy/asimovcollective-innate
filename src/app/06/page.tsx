import type { Metadata } from "next";
import { ColorField } from "@/components/color-field";
export const metadata: Metadata = { title: "06 — Rear grasp", description: "A front-facing pixel knight with a claw reaching around from behind." };
export default function RearGraspStudy(){ return <ColorField key="06" variant="06" />; }
